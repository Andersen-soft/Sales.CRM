// @flow

const SearchingPanelStyles = ({ spacing }: Object) => ({
    searchingPanelWrapper: {
        'marginTop': spacing(2),
        'marginBottom': spacing(2),
        '& div': {
            margin: 0,
        },
    },
});

export default SearchingPanelStyles;
