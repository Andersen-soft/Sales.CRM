// @flow

import { ACTION_LABEL } from 'crm-constants/jss/colors';
import { FONT_SIZE_NORMAL } from 'crm-constants/jss/fonts';

const CRMLabeledFieldStyles = () => ({
    label: {
        color: ACTION_LABEL,
        fontSize: FONT_SIZE_NORMAL,
    },
});

export default CRMLabeledFieldStyles;
