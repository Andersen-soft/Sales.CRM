// @flow

import { UNDERLINE } from 'crm-constants/jss/colors';

const BasisSubscriptionStyles = ({ typography: { fontWeightBold } }: Object) => ({
    link: {
        color: UNDERLINE,
        fontWeight: fontWeightBold,
    },
});

export default BasisSubscriptionStyles;
