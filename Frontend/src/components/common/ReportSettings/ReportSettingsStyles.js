// @flow

const ReportSettingsStyles = ({ spacing }: Object) => ({
    iconButton: {
        padding: 0,
        '&:hover': {
            backgroundColor: 'transparent',
        },
    },
    listPaper: {
        padding: spacing(2.25),
    },
});

export default ReportSettingsStyles;
