// @flow

const ReportUploadStyles = ({ spacing } : Object) => ({
    wrapper: {
        whiteSpace: 'nowrap',
        marginRight: spacing(2),
    },
    icon: {
        marginRight: spacing(),
    },
});

export default ReportUploadStyles;
