// @flow

const CRMTextAreaBlockStyles = ({ spacing }: Object) => ({
    formControl: {},
    headerText: {
        marginBottom: spacing(0.5),
        paddingLeft: spacing(),
        display: 'block',
    },
});

export default CRMTextAreaBlockStyles;
