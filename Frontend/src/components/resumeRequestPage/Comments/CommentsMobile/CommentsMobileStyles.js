// @flow

const CommentsMobileStyles = ({ spacing }: Object) => ({
    container: {
        padding: spacing(2, 1, 1.5, 1),
        position: 'relative',
        height: '100%',
    },
});

export default CommentsMobileStyles;
